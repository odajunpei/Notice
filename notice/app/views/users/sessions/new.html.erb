<div class= "container-fluid">
  <div class= "row">
    <div class= "col-xs-12 col-sm-12 col-md-10 offset-md-1 mt-3">
      <h1>投稿会員ログイン</h1>
    </div>
    <div class="col-xs-12 col-sm-12 col-md-4 offset-md-4">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="field mt-3 text-left">
          <h3><%= f.label :email, "メールアドレス" %></h3>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"form-size" %>
        </div>
        <div class="field text-left">
          <h3><%= f.label :password, "パスワード" %></h3>
          <%= f.password_field :password, autocomplete: "current-password", class:"form-size" %>
        </div>
        <% if devise_mapping.rememberable? %>
          <div class="field">
            <%= f.check_box :remember_me %>
            <%= f.label :remember_me %>
          </div>
        <% end %>
        <div class="actions">
          <%= f.submit "ログイン", class:"btn btn-outline-success", size:20 %>
        </div>
      <% end %>
      <%= render "users/shared/links" %>
    </div>
  </div>
</div>

