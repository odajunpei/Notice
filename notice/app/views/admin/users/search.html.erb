<div class="container-fuluid">
  <div class="row">
    <div class="col-md-8"></div>
    <div class="col-md-4">
      <%= search_form_for @q, url: search_admin_users_path do |f| %>
        <%= f.label :email_cont, 'メールアドレス' %>
        <%= f.search_field :email_cont, placeholder:"メールアドレス" %>
        <% f.submit '検索' %>
      <% end %>
    </div>
  </div>
  <div class="row">
    <div class="col-md-10 offset-1">
      <h1>投稿会員検索結果</h1>
      <table class="table">
        <thread>
          <tr>
            <th>ID</th>
            <th>氏名</th>
            <th>性別</th>
            <th>都道府県</th>
            <th>電話番号</th>
            <th>メールアドレス</th>
            <th><%= sort_link(@q, :created_at, "登録日時") %></th>
          </tr>
        </thread>
        <% @results.each do |user| %>
        <tbody>
        <tr>
          <th>
            <%= user.id %>
          </th>
          <td>
            <%= link_to admin_user_path(user) do %>
              <%= user.name %>
            <% end %>
          </td>
          <td>
            <%= user.gender %>
          </td>
          <td>
            <%= user.address %>
          </td>
          <td>
            <%= user.telephone_number %>
          </td>
          <td>
            <%= user.email %>
          </td>
          <td>
            <%= user.created_at.strftime('%Y/%m/%d %H:%M:%S') %>
          </td>
        </tbody>
        <% end %>
      </table>
      <%= paginate @users %>
    </div>
  </div>
</div>