<div class="container">
  <div class="row">
    <div class="col-10 offset-1">
        <h2 class="mt-3 pb-4">投稿一覧</h2>
      <% @posts.each do |post| %>
      <table class='table table-hover table-inverse'>
        <tbody>
            <tr>
              <td>
                <%= attachment_image_tag post.user, :profile_image, size:"100x100",format: 'jpeg', fallback:"grand.png" %>
              </td>
              <td>
              <%= link_to post_path(post.id) do %>
                <h1><%= post.post %></h1>
                <h2><%= post.created_at.strftime('%Y/%m/%d %H:%M:%S') %></h2>
              <% end %>
              </td>
            </tr>
        </tbody>
      </table>
    <% end %>
    <h3><%= paginate @posts %></h3>
   </div>
  </div>
</div>

